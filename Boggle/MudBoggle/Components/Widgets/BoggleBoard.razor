@using BGL_Library

<MudGrid>
    @for (int i = 0; i < 4; i++)
    {
        @for(int j=0; j<4; j++)
        {
            <BoggleTile Text="@Game.GetGrid()[i,j]"></BoggleTile>
        }
    }
</MudGrid>

@code {
    [Parameter]
    public BoggleGame Game { get; set; }
}
